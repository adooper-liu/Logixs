# @logix/contracts

公共契约运行时载体。当前 P6 仅包含 JSON Schema Draft 2020-12、目录实例和验证 fixtures。

权威入口：

- `schemas/v1/index.json`
- 验证命令：`pnpm contract:check`

TypeScript、OpenAPI、Python 和数据库显式映射属于 P7，当前目录不存在生成产物。
