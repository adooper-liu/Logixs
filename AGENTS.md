# AGENTS.md

本文件是本仓库中所有人工开发者与编码代理的强制执行规则。详细工程标准见
[`ENGINEERING_RULES.md`](./ENGINEERING_RULES.md)。如两者发生冲突，以约束更严格者为准。

## 1. 工作原则

1. 先理解再修改：开始工作前读取相关代码、测试、配置、迁移和文档。
2. 保持范围：只修改实现当前目标所需的内容，不夹带无关重构。
3. 根因修复：禁止用硬编码、临时 SQL、静默默认值或重复逻辑掩盖问题。
4. 保护用户工作：不得覆盖、回退或删除不属于当前任务的改动。
5. 风险验证：先运行最接近改动的检查，只对高风险改动或明确验收要求运行完整质量门禁。
6. 明确事实：不得声称未执行的测试已经通过，也不得隐藏已知风险。
7. 四者协同：业务功能必须同时回答“岗位要完成什么、操作时需要看到什么、系统允许做什么、数据如何可靠保存”，不得只从数据库、接口或字段目录倒推产品方案。
8. 业务可用：数据规范保证事实可信，但不能代替操作流程、信息完整性和交互友好性；称为“工作台”或“闭环”的功能必须让目标岗位完成实际业务结果。

## 2. 开始任务前

- 查找并遵守当前目录及父目录中的 `AGENTS.md`。
- 检查工作树状态，识别已有改动和生成文件。
- 找到模块入口、依赖方向、公共契约和现有测试模式。
- 数据相关修改必须先定位权威 schema 和迁移历史。
- 公共 API、数据库、安全边界或跨模块变更，必须先说明影响范围。
- 仅业务功能、架构、数据、安全、公共契约或需要跨会话交接的任务强制建立 task brief；纯文档、测试稳定性、CI 配置和 Git 维护不单独建立 brief。

## 3. 架构边界

依赖方向固定为：

```text
UI / Transport -> Application -> Domain <- Infrastructure
```

- 路由只声明端点、中间件和控制器绑定。
- 控制器只做输入解析、用例调用和响应转换。
- Application 层负责编排用例、权限和事务。
- Domain 层保存纯业务规则，不依赖 Web、ORM、UI 等框架。
- Repository 只负责持久化，不承载业务决策。
- 外部系统必须经 Port/Adapter 接入，供应商模型不得侵入领域模型。
- 未证明存在独立部署、扩缩容或故障隔离需求时，不新增微服务。
- 禁止跨包引用对方内部路径；跨模块共享内容必须进入正式公共包。
- Web 业务视图和领域组件只依赖稳定 UI 门面；除主题注册入口和测试外，不得直接导入 `themes/*`。
- UI 主题只允许替换令牌、应用壳和无业务判断的展示组件；不得承载权限、状态机、流程合法性、同步事实、费用或 KPI 规则。
- 第三方后台模板默认仅作视觉参考；许可证与来源未核验时，不得复制或执行其 CSS、JavaScript 和资产。详细边界以 `docs/product/UI_SYSTEM.md` 为准。
- 业务功能和 UI 的设计链固定为：`岗位目标 -> 操作流程 -> 所需信息 -> 允许动作 -> 可靠落库与反馈`。前端不得发明权限或业务结论，后端也不得只提供数据而缺少岗位完成工作所需的查询、动作和结果反馈。
- 任务 brief 必须把关键业务步骤逐项映射到界面信息、服务端允许动作和权威事实；不适用 UI 的底层任务须写明后续消费者和承接切片。
- API 可调用、数据已落库、页面能展示均不单独等于业务完成。验收必须覆盖目标岗位能否识别优先事项、理解缺口、执行允许动作、处理失败并确认最终业务结果。

## 4. 数据与迁移

- `database/migrations/` 是唯一迁移入口；禁止在其他目录散落迁移 SQL。
- 数据库对象使用 `snake_case`；代码属性使用 `camelCase`；类型和类使用
  `PascalCase`。
- 数据库、实体和 DTO 之间使用显式映射，禁止依赖偶然同名。
- 已进入共享环境的迁移不可修改，只能追加新迁移。
- 禁止 ORM 在生产环境自动同步 schema。
- 生产数据不得用未纳入版本控制、未经评审的 SQL 修改。
- 数据修复必须包含影响分析、受审计脚本、验证查询和恢复方案。
- 导入必须支持事务、幂等、重复检测、预检和逐行错误报告。
- 非法输入必须明确失败；不得用默认值静默替代未知业务值。

## 5. API 与安全

- 所有输入必须在系统边界进行 schema 校验。
- 所有写接口默认需要认证与授权；Public 写接口必须有明确设计依据。
- 前端路由守卫不是安全边界，权限必须在服务端执行。
- 错误响应使用稳定错误码和追踪 ID，不泄露栈、SQL 或内部结构。
- 列表接口必须分页、限制最大页大小并采用稳定排序。
- 写操作必须定义事务、幂等和并发冲突策略。
- 密钥只来自运行环境或密钥管理系统；不得提交真实凭据。
- 日志不得包含密码、Token、Cookie、私钥或不必要的个人数据。

## 6. 编码规则

- 优先采用仓库现有模式和工具，不引入功能重复的依赖或抽象。
- 一个函数只承担一个可描述职责；复杂业务规则应从控制器和 UI 中提取。
- 公共状态、枚举、事件和 DTO 只保留一个权威定义。
- 时间以 UTC 存储、ISO 8601 交换、用户时区展示。
- 金额采用定点十进制并携带币种，禁止用浮点数表示金额。
- 注释解释原因、约束和取舍，不复述代码。
- TODO 必须包含负责人或跟踪编号以及清晰退出条件。
- 禁止新增无主代码、死代码、调试输出和被注释掉的大段实现。

## 7. 测试要求

- 缺陷修复必须先有能复现缺陷的回归测试。
- 新业务规则必须覆盖成功、失败和边界路径。
- 数据访问使用真实数据库集成测试验证约束和事务。
- API 测试覆盖契约、认证、授权、错误码和幂等。
- 关键用户流程使用端到端测试覆盖。
- 迁移必须验证空库升级、旧版本升级以及关键数据约束。
- 禁止通过跳过测试、放宽断言或降低门槛换取通过。

## 8. 标准命令契约

项目脚本必须逐步提供以下稳定入口：

```text
lint          只检查，不修改文件
lint:fix      显式自动修复
format:check  只检查格式
format        显式格式化
typecheck     类型检查
test          单元和集成测试
test:e2e      端到端测试
build         生产构建
validate      完整质量门禁
```

如果项目尚未配置某项命令，应明确报告“未配置”，不得伪造验证结果。

验证按改动风险分级：

- 低风险：纯文档、文案、测试稳定性或不改变产品行为的 CI 调整。运行最近的检查、`repo:check` 与相关格式检查即可。
- 中风险：单模块生产代码或局部接口实现。运行该模块的 lint、typecheck、test 和 build；涉及关键用户流程时增加相关 E2E。
- 高风险：数据库/迁移、认证授权、公共 API/契约、依赖升级、跨模块规则或核心工作流。运行 `validate` 及对应专项门禁。
- 已通过的完整门禁对未改变的风险面保持有效；后续仅修改文档、测试或 CI 时，不重复运行无关的全量门禁。
- PR 的必需 CI 是合并前最终权威；本地验证用于快速反馈，不与 CI 机械重复。

## 9. 完成任务前

- 检查差异中是否存在无关修改、秘密、调试代码或生成物。
- 按上述风险等级运行受影响检查；只有高风险改动或验收标准明确要求时运行 `validate`。
- 核对数据库迁移、API 兼容性、安全边界和文档影响。
- 报告实际修改、实际执行的检查、未执行项和剩余风险。
- 验证达到当前风险等级要求后即可形成单一主题提交，不为每次中途检查制造提交。
- 本仓库采用持续 Git 授权：可直接执行状态检查、fetch、快进 pull/merge、分支创建/切换、精确暂存、提交、功能分支推送、PR 创建/更新，以及必需检查通过后的 PR 合并，无需逐命令重复确认。
- 持续授权不包含强推、硬重置、覆盖用户改动、删除分支/标签或绕过保护直接写入 `main`；这些操作必须单独明确授权。
- 暂存仍须使用精确路径并审查 staged diff，不得使用 `git add .`、`git add -A` 夹带无关改动。
- “保持工作树干净”只适用于本任务拥有的改动。工作树存在其他人或其他任务的修改时，必须保留并报告，不得为了干净而一并提交、回退、暂存或删除。
- 每批 Git 操作完成后统一核对一次提交标识、上游状态和 `git status --short`；失败时保留现场并如实报告，不得绕过钩子或质量门禁。
- 不得自行部署或修改其他外部系统，除非任务明确要求。

## 10. 禁止事项

- 禁止使用破坏性 Git 命令覆盖现有工作。
- 禁止提交 `node_modules`、构建产物、日志、覆盖率产物和真实环境文件。
- 禁止绕过 CI、关闭安全控制或批量忽略错误。
- 禁止把临时调查文档当作长期架构规范。
- 禁止复制状态机、字段映射、统计口径或权限规则到多个实现中。
- 禁止在没有监控、超时和错误策略的情况下调用外部服务。
