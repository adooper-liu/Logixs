# 工程规则与纪律

## 1. 目标与等级

本规则用于构建可维护、可验证、可审计和可演进的项目底座。

- **MUST**：强制要求，违反时不得合并或发布。
- **SHOULD**：默认要求，偏离时必须在评审中记录理由。
- **MAY**：按项目需求选择。

正确性优先于速度，数据完整性优先于界面表现；所有关键规则最终都应由类型、测试、静态检查、数据库约束或 CI 自动执行。

## 2. 推荐仓库结构

```text
project/
|-- apps/
|   |-- web/                   # 前端应用
|   `-- api/                   # 主 API 服务
|-- services/                  # 有独立部署价值的服务
|-- packages/
|   |-- contracts/             # DTO、枚举、事件和 API 契约
|   |-- domain/                # 纯领域规则
|   |-- config/                # 共享工具配置
|   `-- testing/               # 测试工厂和夹具
|-- database/
|   |-- migrations/            # 唯一迁移入口
|   |-- seeds/                 # 幂等基础数据
|   `-- schema/                # 数据模型说明
|-- infrastructure/            # 容器、监控和部署配置
|-- docs/
|   |-- architecture/          # ADR 和系统边界
|   |-- operations/            # 运维手册
|   `-- product/               # 业务术语和统计口径
|-- scripts/                   # 可复用、可审计脚本
|-- AGENTS.md
`-- ENGINEERING_RULES.md
```

- MUST 使用正式 Workspace 管理多包依赖，并只保留根锁文件。
- MUST 明确每个目录的负责人、公开入口和允许的依赖方向。
- MUST 禁止通过相对路径越过模块边界访问内部实现。
- SHOULD 先采用模块化单体；只有存在客观运行隔离需求时才拆分服务。

## 3. 架构纪律

### 3.1 分层职责

```text
Transport/UI -> Application -> Domain <- Infrastructure
```

- Transport/UI：协议适配、输入输出和交互，不包含核心业务判断。
- Application：用例编排、授权、事务和跨领域协调。
- Domain：实体、值对象、状态机和业务不变量，不依赖具体框架。
- Infrastructure：数据库、消息、缓存、文件和第三方服务实现。

### 3.2 依赖规则

- MUST 让依赖指向领域核心，Domain 不得反向依赖 Infrastructure。
- MUST 通过接口隔离数据库和外部服务。
- MUST 让共享契约具有单一权威来源。
- SHOULD 在模块入口导出公共 API，内部文件默认私有。
- SHOULD 优先组合和小型明确接口，避免万能 Service 和工具类。

## 4. 数据治理

### 4.1 模型与命名

- MUST 为数据库对象使用 `snake_case`，为代码变量使用 `camelCase`。
- MUST 显式定义数据库、实体、领域模型和 DTO 之间的转换。
- MUST 用约束表达非空、唯一、引用关系和合法值范围。
- MUST 使用定点数表达金额并同时保存币种。
- MUST 使用 UTC 保存时间、ISO 8601 传输时间，并显式处理用户时区。
- SHOULD 为关键数据保存来源、导入批次、创建时间和更新时间。
- SHOULD 为审计敏感操作保存操作者、原因和前后值摘要。

### 4.2 迁移

- MUST 将所有迁移集中在 `database/migrations/`。
- MUST 追加迁移，禁止修改已进入共享环境的历史迁移。
- MUST 在生产环境关闭 ORM 自动 schema 同步。
- MUST 在迁移中提供验证方法；破坏性变更必须说明恢复方案。
- MUST 使用 expand -> migrate -> contract 完成不兼容结构变更。
- MUST 让 Seed 可重复执行，并隔离基础数据、测试数据和生产数据。

### 4.3 导入与数据修复

标准处理流程为：

```text
阻止继续污染 -> 保存证据 -> 确定范围 -> 修复根因
-> 添加回归测试 -> 受审计地重放/修复 -> 对账验证
```

- MUST 为导入提供预检、事务、幂等键、重复检测和逐行错误。
- MUST 保存导入批次、来源、统计结果和错误原因。
- MUST 将无法映射的业务值放入待处理队列，不得擅自生成含糊字典项。
- MUST 通过版本化脚本修复生产数据，禁止无记录的手工补丁。

## 5. API 与事件契约

- MUST 对 API 版本化，并用 OpenAPI、GraphQL Schema 或等价方式描述契约。
- MUST 在入口进行结构、类型、长度、格式和业务前置条件校验。
- MUST 统一成功、失败、分页和错误响应格式。
- MUST 为错误提供稳定错误码和追踪 ID。
- MUST 为列表提供分页、最大页限制及稳定排序。
- MUST 为写接口定义认证、授权、事务、幂等和并发策略。
- MUST 为事件提供名称、版本、唯一 ID、发生时间和关联 ID。
- SHOULD 对破坏性契约变更提供兼容期或新版本。
- MUST 禁止将数据库实体直接作为外部契约返回。

## 6. 安全基线

- MUST 默认保护所有写接口，公开写入必须经过威胁分析和明确审批。
- MUST 在服务端执行对象级和操作级授权。
- MUST 从密钥系统或运行环境读取秘密，不得提交真实 `.env`。
- MUST 对密码、Token、Cookie、密钥和个人数据进行日志脱敏。
- MUST 限制上传类型、大小和数量，并在隔离边界解析不可信文件。
- MUST 配置超时、请求体限制、CORS、安全响应头和分级限流。
- MUST 在 CI 执行依赖漏洞、秘密和静态安全扫描。
- SHOULD 建立依赖更新节奏和高危漏洞响应时限。

## 7. 测试策略

测试按风险配置，而不是追求单一覆盖率数字：

- Domain 单元测试：状态机、金额、日期、映射和业务不变量。
- Application 测试：事务、权限、幂等、重试及失败回滚。
- Repository 集成测试：使用与生产兼容的真实数据库。
- API 契约测试：认证、授权、错误码、分页和边界输入。
- 前端测试：关键组件状态、可访问性和核心交互。
- E2E 测试：高价值用户流程，不重复覆盖所有细节。
- Migration 测试：空库升级、已有数据升级和约束验证。
- Contract Parity 一致性测试：共享契约（DTO、枚举、事件、状态与 API/JSON Schema）在 TS/JS 与 Python 等多语言实现之间 MUST 不漂移；由单一权威源生成或派生，并在 CI 运行一致性对比测试，注册项缺失即失败（禁止以放宽断言换取通过）。

缺陷修复 MUST 添加回归测试。测试 MUST 独立、确定、可重复，不依赖执行顺序或共享脏状态。

## 8. 代码质量

- MUST 统一 UTF-8、LF、缩进、格式化和命名规则。
- MUST 将格式化与检查分开；检查命令不得修改文件。
- MUST 开启适合语言的严格类型模式。
- MUST 删除死代码、调试输出和无退出条件的临时开关。
- SHOULD 控制函数和模块职责，按业务能力拆分而非按技术杂项堆放。
- SHOULD 为复杂决策记录原因，不为显然代码写叙述性注释。
- MUST 通过代码生成或公共包消除跨端枚举与状态规则复制。

## 9. CI/CD 质量门禁

每个合并请求 MUST 通过：

1. 锁文件确定性安装。
2. 格式检查和 Lint。
3. 类型检查。
4. 单元及集成测试。
5. 生产构建。
6. 迁移验证。
7. 依赖、秘密和静态安全扫描。

主分支 MUST 受保护。禁止绕过失败检查；紧急例外必须留下审批、风险、补救任务和时限。

发布 SHOULD 使用不可变制品，同一制品逐环境晋级，禁止在服务器现场构建或修改代码。

## 10. Git 与评审

- MUST 保持短生命周期分支和可评审的小批次改动。
- MUST 让提交表达一个完整主题，并说明改变原因。
- MUST 在 PR 中描述问题、方案、风险、数据影响、验证和回滚方式。
- MUST 由责任人评审数据库、安全、公共契约和共享领域规则变更。
- MUST 在多执行器/多会话协作时，以任务 brief 作为交接与进度载体：状态存于文件顶部 YAML frontmatter（`design | coding | review | fix | blocked | done`），同一时刻只允许一个任务处于进行中；无测试/构建等验证证据不得标 `done`。模板见 `docs/planning/tasks/_template.md`。
- MUST 标记生成文件及其生成命令。
- MUST 禁止提交依赖目录、构建产物、日志、覆盖率、真实环境配置和本地数据。

## 11. 可观测性与可靠性

- MUST 使用结构化日志，并包含服务、环境、事件名和关联 ID。
- MUST 监控吞吐、延迟、错误率、资源、队列和外部依赖。
- MUST 区分存活检查与就绪检查。
- MUST 为外部调用设置超时，并根据语义选择重试、熔断和降级。
- MUST 让异步任务可追踪、可重试且可去重。
- MUST 为告警提供影响说明和处理手册。
- MUST 定期执行备份恢复演练；未验证恢复的备份不视为有效。

## 12. 文档与决策

文档按消费关系管理，先有消费者再建文档：

- README MUST 只描述当前有效的启动、验证和系统入口。
- 架构决策 MUST 通过 ADR 记录背景、选择、替代方案和后果。
- 业务术语与统计口径 MUST 指明权威来源和负责人。
- 每份文档 MUST 能回答「谁读、何时读、怎么用」；没有明确消费者就不新建，失去消费者即标「已过时」或删除。
- 文档 MUST 分层管理：
  - 运行时文档（契约、Schema、迁移、Seed、Fixture、CI 与生成物等会被执行消费的）MUST 与代码同步，错误直接影响行为。
  - 参考文档（架构、流程、术语等）可「尽力准确」，但 MUST 标注状态（已定 / 候选 / 已过时）。
- 单一真相 MUST 不复制：同一事实只写在一处，其他文档用相对链接引用而非重复正文。
- 新建文档 MUST 先过三问：能并进现有文档吗？消费者是谁？谁来维护？
- 参考文档超过 6 个月未维护 MUST 标「已过时」或删除；已完成任务对应的任务 brief 保留但不再修改。
- 文档 MUST 纳入格式、链接和过期检查；`docs/README` MUST 维护文档消费链（谁在何时读）。
- 临时调查记录 SHOULD 归档，不得混作当前规范。
- 文档与实现冲突时，先以可执行契约和代码确认事实，再立即修正文档。

## 13. 完成定义

一项工作只有满足以下条件才算完成：

- 验收行为完整，成功、失败和边界路径均已考虑。
- 模块边界、公共契约和向后兼容性没有被意外破坏。
- 数据迁移、数据修复及恢复策略已经处理。
- 自动化测试与生产构建通过。
- 认证、授权、隐私和秘密风险已经检查。
- 日志、指标和错误信息足以支持生产排障。
- 文档与行为一致。
- 不存在隐藏默认值、临时补丁、无主 TODO 或未披露风险。

## 14. 例外管理

规则例外必须写入 ADR 或 PR，至少包含：

- 被偏离的规则及原因。
- 风险和影响范围。
- 临时保护措施。
- 负责人和终止日期。
- 恢复合规的跟踪任务。

“时间紧”“历史如此”或“本地可运行”不能单独构成例外理由。
