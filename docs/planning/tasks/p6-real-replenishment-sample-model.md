---
status: done # design | coding | review | fix | blocked | done（机器可校验）
branch:
verification: docs/product/domain/evidence/REAL_REPLENISHMENT_SAMPLE_VALIDATION_20260921.json；2026-09-21 `corepack pnpm repo:check` 通过，Python py_compile 与 demo SQL/fixture 静态核对通过；Prettier 因本机依赖未安装未执行
---

# 任务：真实备货样本字段、关系与演示数据

## 目标

基于 `D:\aosom\Downloads\备货数据` 的真实文件，形成从备货单、SKU 到集装箱的全生命周期字段归属、样本内逻辑关系，以及可重复迁移到当前数据库的演示数据。所有结论保留来源与原值，并用跨文件对账防止失真。

## 边界 / 不做

- 权威边界遵守 `AGENTS.md`、`ENGINEERING_RULES.md`、`docs/INDEX.md`、正式生命周期节点目录、目标字段目录和当前 `database/schema.prisma`。
- 样本文件是现状证据，不自动晋升为正式业务规则；无法由多个来源或仓库权威文档证明的关系明确标为观察或待确认。
- 不修改源文件、历史迁移、公共 API、运行时状态机或生产数据。
- 演示数据只使用样本中出现的业务值；系统必需但源文件不存在的技术字段使用明确、可审计的 demo 命名空间值。
- 对当前 schema 无正式列承载的字段保留在来源快照/证据清单，不伪造列或丢弃原值。

## 验收

- [x] 每个源文件有 SHA-256、工作表/页/附件清单和解析结果记录。
- [x] 字段目录覆盖业务相关源列，给出定义、粒度、生命周期节点、来源和当前承载建议。
- [x] 关系分析覆盖备货单、主备货单、SKU、出运计划、订舱/提单、箱号和报关资料的基数与实例对账。
- [x] demo seed 使用确定 ID 与 `ON CONFLICT`，并按当前 Prisma 物理列、类型、枚举和约束生成。
- [x] 对关键数量、箱数、件数、毛重、体积、金额和标识符执行跨文件交叉验证，差异未被静默修正。
- [x] `corepack pnpm repo:check`、Python 语法检查和专用样本校验通过；Prettier 因本机依赖未安装未执行并如实记录。

## 方案

1. 只读抽取 Excel、PDF、ZIP、图片和旧 Word，生成文件/页签级证据索引与标准化单元格快照。
2. 依据正式 14 节点目录和目标字段目录，整理全生命周期字段、定义、粒度与节点归属。
3. 以真实标识符做跨文件连接，记录已证明的主外键候选、基数、汇总关系、冲突和未知项。
4. 基于当前 schema 生成幂等 SQL seed；完整源字段通过 `import_batch` / `import_row.snapshot` 保真，正式模型只写已具备权威映射的字段。
5. 用独立校验脚本从源文件重新计算关键事实，并核对文档与 seed。

## Review notes

待完成后填写。

## 进度 log

| 日期       | 阶段   | 负责 | commit | 说明                                                                   |
| ---------- | ------ | ---- | ------ | ---------------------------------------------------------------------- |
| 2026-09-21 | coding | —    | —      | 完成仓库与源目录盘点，开始只读抽取与对账                               |
| 2026-09-21 | done   | —    | —      | 44 文件清单、字段目录、关系分析、demo fixture/SQL 与 21 项独立对账完成 |
