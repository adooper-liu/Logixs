---
status: done # design | coding | review | fix | blocked | done（机器可校验）
branch: feat/work-execution-first-slice
verification: 本地验证（2026-09-16）：AI pytest 6 passed；pnpm validate 全部通过，API 89 文件 / 405 项、Web 52 文件 / 173 项、E2E 50 passed / 7 skipped，生产构建通过；运行态 Web/API/AI 均返回 200，AI 实时请求未再误映射。
---

# 任务：真实导入样本的文件名与映射边界修复

## 目标

修复真实 52 列导入样本暴露的两个边界缺陷：multipart 中文文件名乱码，以及 Mock 映射用子串误匹配不相关列。确保建议保持“可审、可拒绝”，不把模糊猜测伪装成高置信事实。

## 边界 / 不做

- 不自动确认映射，不替操作人猜测缺失的出运数量单位。
- 不覆盖当前批次的原始 AI 建议或历史记录。
- 不修改目标字段目录、预检规则、数据库结构或生命周期状态。

## 证据

- 本地真实批次 `c3319c06-112b-44f4-8fbc-bb3595a162de` 已解析为 98 行 / 52 列。
- 文件名以 UTF-8 字节被 Latin-1 解读后落库；“备货单备注”“备货单状态”“FOB 单价”被误建议为 `orderNumber`。

## 验收

- [x] UTF-8 中文 multipart 文件名恢复正确；ASCII、原生 Unicode 与合法 Latin-1 名称不被破坏。
- [x] 精确别名继续命中；仅包含“备货单”等短词的不相关列返回未识别。
- [x] 同一目标字段被多列建议时，前端不预选任何冲突列。
- [x] AI 服务加载修复后，映射能力回归测试通过。
- [x] `pnpm validate` 通过。

## 方案

文件名修复放在 Controller 输入边界，仅在原字符串全为单字节字符且对应字节可严格解码为 UTF-8 时恢复。Mock 映射移除无边界子串匹配，只使用版本化字段目录的大小写无关精确别名；未知列显式留空给人工处理。

## 进度 log

| 日期       | 阶段   | 负责 | commit | 说明                                   |
| ---------- | ------ | ---- | ------ | -------------------------------------- |
| 2026-09-16 | coding | —    | —      | 真实批次只读核验后建立文件名与映射回归 |
| 2026-09-16 | done   | —    | —      | 输入边界、精确映射与 UI 冲突防护已验证 |
