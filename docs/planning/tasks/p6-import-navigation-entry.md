---
status: done # design | coding | review | fix | blocked | done（机器可校验）
branch: main
verification: 2026-09-16 本地 `pnpm validate` 通过（API 84 文件/384 项、Web 50 文件/167 项、Worker 5 项、E2E 50 通过/7 跳过，生产构建通过）。
---

# 任务：智能导入主导航入口

> 唯一交接载体。同一时刻仅一个进行中任务。

## 目标

让已完成的智能导入闭环可从作业壳主导航进入，不再要求用户手输 `/import`。

## 边界 / 不做

- 只补 `/import` 的导航元数据、图标映射、测试和对应产品快照，不改导入业务流程或服务端权限。
- `/import/:batchId` 仍由上传后下钻进入，不单独占用侧栏入口。
- 当前演示角色只裁剪菜单，不作为生产环境认证或授权依据。

## 验收

- [x] 现场、计划和经理演示角色均能看到“导入货柜”，且链接到 `/import`。
- [x] “导入货柜”排在“干活”之后，详情路由不重复进入导航。
- [x] Web 受影响测试、类型、Lint、格式和仓库规则通过。
- [x] 完整 `pnpm validate` 通过。

## 方案

给 `/import` 补齐 `navLabel`、`navIcon`、`navOrder` 和 `roles`；复用 Lucide `FileUp`；同步 UI 页面清单并增加导航回归测试。

## Review notes

（缺陷优先）

## 进度 log

| 日期       | 阶段   | 负责 | commit | 说明                         |
| ---------- | ------ | ---- | ------ | ---------------------------- |
| 2026-09-16 | coding | —    | —      | 开工：补智能导入主导航入口。 |
| 2026-09-16 | done   | —    | —      | 导航回归及完整质量门禁通过。 |
