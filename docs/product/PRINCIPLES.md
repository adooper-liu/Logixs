# Logix 产品原则（负责人坚持，2026-09-06）

> 状态：**负责人确认（本批方案不可漂移的基线原则）** · 2026-09-06。
> 定位：任何设计取舍都不得违背以下原则清单与核心运行模型；冲突时以本清单为准，并回查各领域文档口径。
> 关联：`PRODUCT_BRIEF`、[UX_CONTAINER_WORKBENCH](./UX_CONTAINER_WORKBENCH.md)、[NODE_PDCA](./domain/NODE_PDCA.md)、
> [CONTAINER_LIFECYCLE](./domain/CONTAINER_LIFECYCLE.md)、费用/滞港费见下映射。

## 0. 元治理：确认 ≠ 正确（M0）

负责人确认（sign-off）仅代表**业务意图明确**与**范围/优先级授权**，不自动证明方案在领域一致性、行业规范（S）、数据可靠性与现场可操作性上正确。方案定案必须依次通过 **冲突分析 → 边界验证 → 样本回验**（回验见 [NODE_PDCA_VALIDATION](./domain/NODE_PDCA_VALIDATION.md) F1–F8），评审合格后才升格为 `Decision` 进入正式方案；文档不得仅凭"负责人确认"状态戳标注正确性。
分级：**O**=已确认业务目标/事实；**S/R**=行业规范与现网证据；**C**=待验证设计候选；**Decision**=冲突分析、边界验证、样本回验通过后进入正式方案。

## 十原则（P1–P10）

| #   | 原则                                                                                                                                                                                                  | 落点                                                                                                               |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| P1  | **可视化**：货柜抽象为可视化实体，单屏一眼看尽全生命周期状态/动态/异常与"下一步动作"；动作一键确认执行                                                                                                | [UX_CONTAINER_WORKBENCH](./UX_CONTAINER_WORKBENCH.md)（rail/节点/标记/异常/动作中心；V1 随 P6）                    |
| P2  | **任务驱动与闭环工作流程（PDCA）**：每个关键节点都走 计划→派发工单/任务→Check→改善 的闭环                                                                                                             | [NODE_PDCA](./domain/NODE_PDCA.md)（14 个生命周期节点的任务拆分候选、工单对象、预警规则）                          |
| P3  | **货柜全生命周期管理**：业务主流程覆盖备货→装箱→出运→…→还箱；当前实现从已出运货柜列表起步，未来向计划/采购等上游延伸；时间/状态/密封/来源权威一体                                                     | [CONTAINER_LIFECYCLE](./domain/CONTAINER_LIFECYCLE.md)、[LIFECYCLE_CONSISTENCY](./domain/LIFECYCLE_CONSISTENCY.md) |
| P4  | **超期费用管理（业务俗称“滞港费”）**：Demurrage、Detention、Storage 为核心能力——免费期/费率标准、产生/预警/对账/一键处置闭环                                                                          | 见 §2（类型边界已确认，具体合同规则待样本）                                                                        |
| P5  | **时序：写入顺序 ≠ 发生顺序**：事件可先于任务（事后补录）、可乱序/迟到/回填；撤船/甩柜/查验/返工等按事件分支落账，经对账与人工纠偏收敛；分支为合法转换，非错误码                                      | [LIFECYCLE_CONSISTENCY](./domain/LIFECYCLE_CONSISTENCY.md)（补 R7–R9）、CONTAINER_STATUS_MODEL（补合法转换）、§1.7 |
| P6  | **权威：入口权威 ≠ 字段权威**：已出运货柜列表是“哪些货柜进入系统”的入口权威，不是后续字段的最终权威；每条事实带来源+权威，字段冲突按显式策略裁决并留痕，禁止“以列表为准”一刀切                        | [INTEGRATION_BOUNDARIES](./domain/INTEGRATION_BOUNDARIES.md)（字段级来源权威+冲突处理）、§1.7                      |
| P7  | **确认三档：服务器确认 ≠ 业务完成**：以 ①请求已接收 ②业务已接受 ③业务事实已落账 区分；UI 按操作处呈现对应档位，禁止笼统“成功”；离线/超时保留“未确认”，幂等可重试                                      | §1.1 最小三段提交确认、[ERROR_CODES_CATALOG](./domain/ERROR_CODES_CATALOG.md)与契约回执                            |
| P8  | **费用类型化：禁止单公式固化**：Demurrage/Detention/Storage 各自定义责任段、起算/截止事件、免费期、自然日/工作日、阶梯费率与账单权威；预估/应计/账单/审核确认分离                                     | §2、[FEE_DEMURRAGE](./domain/FEE_DEMURRAGE.md)（类型化规则集）                                                     |
| P9  | **复核触发：按风险/冲突触发，非全节点统一审批**：人工复核是例外路径，常规作业不多设审批层；复核结论、会议决议、AI 建议分开留痕、不覆盖事实；触发规则配置化并可用结果反馈学习                          | §1.7、[NODE_PDCA](./domain/NODE_PDCA.md) ⑥异常/⑦会议                                                               |
| P10 | **表单即证据：单事实多投影**：原表单是 R 级证据与需求素材，不是表结构或菜单模板；统一对象模型上给 任务视图（现场）·计划视图（计划/采购）·管理投影（管理层），一次采集多处复用，禁止重复填报原样电子化 | §1.8、[DATA_MODEL_P2-06](./domain/DATA_MODEL_P2-06.md)、[UX_CONTAINER_WORKBENCH](./UX_CONTAINER_WORKBENCH.md)      |

## 1. 核心运行模型：三种状态 + 三层映射（负责人 2026-09-06）

### 1.1 三种状态（必须独立记录）

| 状态                                | 对员工的业务作用                                     | 权威与记录要求                                                                                               | 不得混同                                                                                     |
| ----------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| 货柜状态 `currentStatus`            | 说明货柜实际走到哪，并约束当前允许进入哪些作业       | 由实际事件/证据经货柜状态机推进；保存当前值、事件历史、发生时间、来源与证据                                  | 状态本身不是工作指令；必须结合计划、标记、异常、权限和任务规则生成“现在要做什么”             |
| 任务状态（候选契约名 `taskStatus`） | 说明这项工作做到哪；任务结果与证据回答具体做到了什么 | 由任务/工单生命周期推进；保存当前值、领取/执行/报工/异常历史、人员、时间、结果与证据                         | 不复用货柜状态码；单靠“已完成”不能替代完成结果与证据；具体字段名与状态集在任务领域模型中定稿 |
| 同步状态（候选交互名 `syncStatus`） | 证明本次动作与服务端记录是否一致                     | 由客户端待提交记录与服务端回执共同确定；至少关联 `clientOperationId`、动作码、幂等结果、`traceId` 与确认时间 | 不能用“同步成功”代表任务完成、货柜推进或现场事实真实；具体字段名与状态集待契约定稿           |

三种状态通过 `containerRecordId + taskId + clientOperationId/traceId` 关联，但分别拥有独立的取值语义、历史与审计；货柜与任务各走自己的业务状态机，同步状态由待提交记录和服务端回执驱动。服务端先确认扫描/报工请求，再校验任务规则与证据；只有校验通过并产生合法业务事件，才允许推进货柜状态。

| 关联层级                          | 基数     | 含义                                                                                       |
| --------------------------------- | -------- | ------------------------------------------------------------------------------------------ |
| `ContainerRecord -> Task`         | `1:N`    | 一次货柜流转会在不同节点产生多个任务；某个任务完成不能冒充整柜流程完成。                   |
| `Task -> ClientOperation`         | `1:N`    | 一个任务可发生领取、证据确认、提交、重试等多次操作；同步状态必须指明是哪一次操作。         |
| `ClientOperation -> Result/Event` | `1:0..N` | 操作可能被拒绝、只形成任务结果，或落下一个及以上合法业务事件；不能假定每次操作都推进货柜。 |

三种状态与三段确认**不得按位置一一对应**。三段确认描述的是一次 `ClientOperation` 从请求接收到事实落账的处理进度；业务接受或事实落账后，服务端可按任务规则更新任务结果。只有落账结果包含映射有效、状态转换合法的货柜事件时，才推进货柜状态。任务完成但货柜不推进、权威货柜事件先到而任务后补，均是合法场景。

🗣️ 白话：柜况限定“现在能做哪些事”，任务明确“这次要做什么、做到哪”，结果与证据说明“实际做到了什么”，同步回执证明“这次动作有没有被系统按原样可靠记录”。它们互相关联，但不是同一个进度条。

#### 从货柜状态到动作记录

```text
货柜状态 + 计划/标记/异常/权限
                ↓ 服务端规则推导
任务定义 + 当前动作
                ↓ 员工或系统执行
ClientOperation（同一操作编号、动作码、对象、操作者、发生时间、载荷摘要）
                ↓ 请求接收 → 业务接受 → 事实落账
任务结果/证据 + 合法业务事件 → 更新任务与货柜投影
```

“动作即记录”是审计纪律：每个会改变归属、证据或业务结论的动作都必须形成可追踪的 `ClientOperation`，按钮文案、动作码、对象和操作编号在确认与回执中保持一致；重试复用原操作编号。被拒绝、超时或技术失败的尝试也保留操作记录，但不得冒充已完成的任务结果或已落账的业务事实。纯展开、筛选等界面操作不属于业务动作。

#### 最小三段提交确认（负责人 2026-09-06）

| 段             | 回答的问题                                                | 成立条件                                                                                      | 对业务状态的影响                                   |
| -------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 请求已接收     | 服务器是否可靠地收到了这次操作                            | 服务端已持久识别 `clientOperationId`/幂等键并可供查询或重放；单纯网络连接或未形成可靠回执不算 | 不代表业务通过，不推进任务或货柜状态               |
| 业务已接受     | 权限、任务归属/并发、当前状态、业务前置条件和证据是否通过 | 服务端完成确定性校验并明确接受；拒绝时返回稳定业务原因                                        | 表示可以执行或已进入受控执行，但仍不等于事实已落账 |
| 业务事实已落账 | 合法结果是否已经提交到权威记录                            | 事务提交完成，并可返回结果引用、事件/版本、提交时间和追踪 ID                                  | 此时才允许更新任务结果、货柜事实及达成/绩效投影    |

三段是一个提交结果的**最小语义边界**，不是要求所有实现都增加三个持久化状态或让用户等待三次。若业务校验与落账在同一同步事务内完成，后两段可以在一次响应中同时成立；若经过异步队列、外部调用或跨事务处理，才需要保留“已接受、待落账”的可查询阶段。

只在以下至少一项不同时才进一步区分：责任主体、事务边界、重试/补偿方式、用户下一步动作、SLA 或审计证据。内部队列跳转、线程步骤和对用户无影响的技术细节不得扩散为业务状态。

#### 同类语义的适度拆分清单

> 这是一份语义核对表，不是要求为每一格新建状态码、字段或页面。先保留事实、时间和证据；只有差异会改变权限、后续流程、责任、恢复、SLA、统计或审计结论时，才升格为可见状态。

| 场景       | 必须避免混同                                       | 最小处理原则                                                                                                     |
| ---------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 时间       | 计划、预计、实际；业务发生时间与提交/接收/落账时间 | 计划用于承诺，预计用于滚动判断，实际来自发生证据；离线或迟报时同时保留业务发生时间与系统确认时间，不互相覆盖     |
| 任务作业   | 已分配、已领取、员工已报工、业务结果已落账         | 员工报工只是一次提交，不能直接写成任务完成；是否把分配/领取/开始分别建状态，由排他领取、计时、交接和恢复规则决定 |
| 单证/指令  | 已生成、已发送、对方已送达、对方业务已接受         | 没有可信回执只能声称“已发送”；送达不等于清关行/承运商已接受办理，只有确有下游依赖或追责要求时才增加后两段        |
| 外部里程碑 | 外部已报告、证据已校验、内部事实已落账             | 来源报告不能直接冒充内部权威事实；若可信源映射、校验和落账在同一事务完成，可只展示最终结果，但仍保留来源与证据   |
| 异常       | 已上报、已受理/分派、已解决、已验证关闭            | “给出处理结果”不等于影响已经消除；低风险且同一责任人可按规则合并解决与关闭，高风险或需复核时必须分开             |
| 费用       | 计划/预算、预计、账单/索赔、审核确认、支付         | 预计费用不得称为实际成本；系统只展示当前有权威依据的阶段，支付不在本系统责任边界时只记录外部引用，不伪造支付状态 |
| 改善决议   | 已决议、已分派、已执行、效果已验证                 | 执行动作不自动证明改善有效；只有存在效果指标和复核责任时才单列“效果验证”，否则保留执行结果即可                   |

建模时优先记录各事实的 `occurredAt/source/evidence/actor` 与关联 ID；不要为了 UI 徽章数量把同一事务中的瞬时步骤固化为长期状态，也不要因为页面想简洁而把会改变业务结论的事实压成一个“完成”。

### 1.2 三层模型：维度 × 节点 × 动作

| 层       | 结构角色                                     | 核心内容                                                                                                         | 映射与关联                                                                                       |
| -------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 管理层   | **维度**：从不同管理视角切分、聚合和比较     | 进度、预警、异常、风险、绩效、改善、资源规划与管理                                                               | 维度落到具体节点与动作，可下钻到货柜事实、任务、证据和回执；改善结果受控反馈到计划/规则/资源配置 |
| 主流程层 | **节点**：定义货柜生命周期所处位置与先后     | 从备货单建档到还空箱的节点、计划/预计/实际时间、事件、状态、标记、单证、费用与来源证据                           | 节点提供可写窗口、任务条件和业务上下文；接收作业动作经校验产生的合法事件与数据更新               |
| 作业层   | **动作**：定义员工或系统在某个节点具体做什么 | 任务上下文、前置条件、可执行动作、证据采集、结果提交和异常恢复；领取、物料、扫描、传送、报工只是按任务选用的能力 | 每个动作必须挂到节点/货柜；不直接改货柜状态，经服务端校验后影响主流程，并被管理维度统计          |

```text
管理层：维度（进度/预警/异常/风险/绩效/改善/资源）
                         ×
主流程层：节点（备货单 → ... → 还空箱）
                         ×
作业层：动作（任务上下文 → 动态动作/证据 → 结果确认；异常可从任意阶段进入）
```

三层不是三条互不相干的流程，而是同一货柜上下文的映射：一个管理维度可以观察多个节点与动作；一个节点可以产生多个任务/动作；每个动作必须关联一个业务节点，并把结果反馈给主流程和管理投影。这里的“×”表示关联坐标，不表示为所有维度、节点和动作生成无效的笛卡尔积组合。

### 1.3 数据、信息、状态与时序

| 要素 | 三层中的位置                                                               | 约束                                                                           |
| ---- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 数据 | 节点事实、动作记录、扫描/报工证据、异常、同步回执                          | 保留原始值、来源、操作者、时间与关联 ID；历史不可由管理报表反向改写            |
| 信息 | 面向员工/管理者的任务上下文、进度、预警、风险、绩效与资源投影              | 由数据按管理维度聚合或解释，必须可下钻回原始事实和动作                         |
| 状态 | 主流程节点消费 `currentStatus`；作业动作消费任务状态与同步状态             | 三种状态独立记录，依映射关联，不合并成一个笼统状态                             |
| 时序 | 主流程按生命周期节点顺序；作业按任务/动作发生顺序；管理按统计时点/周期观察 | 区分计划/预计/实际与发生/提交/确认时间；预计不推进业务状态，未确认提交不计完成 |

### 1.4 闭环硬约束

- UI 必须在相关操作处分别呈现货柜状态、任务状态与同步状态，不能只显示一个笼统的“成功”。
- UI 的当前动作必须来自服务端授权任务，不得仅凭货柜状态在前端自行推导；任务完成时保留结果与证据，不能只改一个状态徽章。
- 每次会改变归属、业务结果或证据的作业提交必须幂等、可追踪、可重试；离线/超时时保留“未确认”事实，不伪装完成。领取、物料确认、扫描、报工等仅在任务定义要求时出现。
- 业务动作从提交开始即绑定 `clientOperationId + actionCode + containerRecordId/taskId`；确认页、回执和审计记录必须能证明它们指向同一次动作。
- 提交反馈至少遵守“请求已接收 → 业务已接受 → 业务事实已落账”的语义边界；业务拒绝不是同步失败，已接收后的处理超时也不得提示员工重新生成操作 ID。
- 作业成功路径、拒绝、重复提交、并发占用、异常阻塞、恢复/返工均须在任务领域模型与契约中定义。
- 任一管理维度、主流程节点和作业动作都必须能沿映射关系相互定位；禁止产生无法关联节点的任务或无法下钻事实的指标。
- 管理指标必须能下钻到货柜事实、任务记录与同步回执；绩效不得使用未被服务器确认或未通过业务校验的提交。

### 1.5 当前实施边界与演进方向

| 范围         | 已确认口径                                                                       | 设计约束                                                                                                                            |
| ------------ | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 当前数据起点 | 备货单完成出运转换后形成的**已出运货柜列表**；它是当前系统所有后续节点的数据起点 | 首期 UI 与作业闭环从该列表进入，向后跟踪至还空箱；备货单保留为来源身份与追溯锚点，但当前不在本系统内执行上游计划/采购/备货/订舱作业 |
| 当前接入方式 | 文件导入                                                                         | 必须经过标准化、预检、审核、幂等、对账与审计，不得因其为过渡渠道降低可靠性要求                                                      |
| 后续接入方式 | 与上游系统直接进行数据/系统集成                                                  | 文件导入与直接集成是可替换的接入适配器，必须进入同一标准化业务写入边界；切换渠道不得改变下游货柜身份、状态历史和 UI 口径            |
| 未来前端边界 | 向上游扩展为计划→采购→备货→订舱→出运，并与现有出运后链路连成端到端流程           | 通过增加上游节点、动作与管理投影扩展，不复制或重建出运后的货柜事实链                                                                |

这里必须区分两种边界：**业务主流程口径**仍从备货单延续到还空箱；**当前实施/上线口径**从“已出运货柜列表”开始。未来前端向计划与采购延伸，是把现有起点向前移动，不是另建一套下游流程。

### 1.6 每个节点的七组 SOP 完整性框架（负责人 2026-09-06）

| 组                   | 节点必须回答               | 内容边界                                                                                                                      | 主要数据关系                                                        |
| -------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| ① 计划与能力分解     | 做什么、用多少运力         | 年度资源规划；按月/周/日分解；分配具体服务商、人员、车辆/运力与计划日期                                                       | 上层能力与目标逐层分解为节点计划和任务                              |
| ② 执行与达成跟踪     | 实际做了没有、做到了什么   | 按任务类型组合分配/领取、前置检查、资料/资源准备、指令/传送、实物操作、证据采集、等待外部、结果提交与恢复；计划与实际达成对比 | 计划行 → 任务定义/实例 → 动作与证据 → 已确认执行结果                |
| ③ 货柜全生命周期状态 | 柜子走到哪了               | 一柜一档的节点事实、事件、证据和 `currentStatus`                                                                              | 货柜记录贯穿各节点；实际事件才允许推进货柜状态                      |
| ④ 周期时效分析       | 花了多长时间               | 相邻节点和分段的计划/预计/实际时间差，如海运、清关、拖卡、卸柜周期                                                            | 只从③的权威时间戳派生，不另填一套 KPI 时间                          |
| ⑤ 费用结算           | 花了多少钱                 | 正常费用、Demurrage/Detention/Storage、异常费用，以及预计/应计/账单/审核/支付阶段；按柜与管理周期聚合                         | 金额使用定点数+币种，费用关联货柜、节点、期间、合同规则、账单与来源 |
| ⑥ 异常与反馈         | 出问题怎么办、如何持续改善 | 异常上报、分派、处理、跟进、恢复、根因和规则/价格/资源反馈                                                                    | 异常与货柜状态正交；处置结果回流①计划与规则配置                     |
| ⑦ 会议机制           | 如何复盘和作决策           | 报表、绩效、Sub./HQ 物流例会、供应商月度会议及决议跟踪                                                                        | 读取③④⑤及⑥的事实和指标；会议决议形成改善动作并回到①                 |

七组是**领域设计与验收时的完整性检查框架**，不是页面信息架构，也不表示每个节点都复制七张物理表、七个对象或七个固定页签。节点设计要说明各组“适用/不适用/继承”的关系；运行时只实例化真正发生的计划、任务、事件、费用、异常或决议，不能为了填满七格制造空记录。

| 角色/场景 | 默认只呈现                                                                                    | 按需展开                                               |
| --------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 现场员工  | 当前任务上下文、可执行动作、任务要求的资料/资源与证据、结果或异常入口，以及当前操作的同步反馈 | 与当前动作直接相关的货柜节点、前置条件、证据和处理原因 |
| 计划/调度 | 能力、月/周/日计划、服务商分配、计划与达成差异                                                | 柜级任务、前置条件、异常与资源缺口                     |
| 运营/客服 | 货柜状态、周期、异常、单证和待协调事项                                                        | 任务证据、来源冲突、费用影响                           |
| 财务/管理 | 费用、绩效、风险、趋势与会议决议                                                              | 下钻到货柜、节点、任务、账单和原始证据                 |

同一用户可因角色与当前任务看到不同组合；隐藏不相关信息不等于删除数据关系。员工最小操作闭环优先于展示框架的完整性。

```text
①计划/能力 → ②执行/达成 → ③状态沉淀 → ④周期 + ⑤费用
      ↑                                          ↓
      └──── ⑦会议复盘 ← ⑥异常/反馈/改善 ────────┘
```

- ③是贯穿全部表单的主数据流；柜号是装箱后到卸柜前的共享业务追踪线索，但不是所有表的数据库主键，内部关联仍须使用稳定的货柜流转记录 ID，并保留备货单来源锚。
- ①的年度能力必须逐层分解到月、周、日和具体责任主体；下游计划必须显式引用其前置条件，例如到港计划是拖卡/卸柜计划的输入。
- ②中的“做到”只认服务端确认且通过业务校验的任务结果；客户端待同步记录不能计入达成。
- ④和⑦是管理投影；⑤中的费率、合同与实际账单是独立业务事实，预计费用和聚合结果是派生信息。它们都不得反向覆盖③的货柜事实；人工复核结论、会议决议与 AI 建议必须分开留痕。
- 会议本身不是作业表单流或货柜节点；它消费报表并产生可跟踪的决议动作。

### 1.7 非线性事件与风险触发复核

“计划 → 执行 → 状态 → 分析”是 PDCA 的因果结构，不是系统只能按此顺序收数。实际运行允许以下情况存在，但必须保持可解释和可恢复：

| 情况                             | 系统处理                                                                                             | 禁止做法                                         |
| -------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| 权威状态事件先到、员工任务后补   | 先按来源权威和状态规则落业务事件，再对未完成/缺失任务生成对账差异或补充动作                          | 为了等任务而压住真实货柜状态，或倒填员工已完成   |
| 事件乱序或迟到                   | 保留业务发生时间与接收时间；去重后按事件序列重算投影，必要时进入冲突复核                             | 用接收顺序冒充发生顺序，直接覆盖历史             |
| 撤船、甩柜、查验、返工或计划变更 | 作为事件/异常分支追加记录，并驱动重新计划、任务取消/替换或恢复动作                                   | 把异常硬折叠成正常主状态，或删除原计划与原事件   |
| 外部更正/撤回                    | 追加引用原事件的更正/撤回事实并重放可重建投影；若会使已密封状态倒退，则生成不一致/补偿任务并进入复核 | 原地篡改已被消费的历史事实，或自动回退已密封状态 |
| 多源事实冲突                     | 按字段/事件类型的权威策略自动采纳、拒绝或进入人工复核，并产出对账结果                                | 使用“最新到达”或全局来源排名静默覆盖             |

人工复核只在风险或冲突改变业务结论时触发，例如未知映射、权威来源冲突、密封历史纠正、关键证据缺失、高风险状态推进、重大费用差异或需独立验证的异常关闭。已知映射、同值重复、幂等重放、低风险且规则可确定的正常动作不得统一加审批。具体风险等级、金额阈值和复核权限保持候选，须用真实样本校准。

### 1.8 证据分级与正式决策门禁

| 标记       | 含义                                                   | 可以证明什么                             | 不能直接证明什么                 |
| ---------- | ------------------------------------------------------ | ---------------------------------------- | -------------------------------- |
| `O`        | 负责人确认的业务目标、约束或事实                       | 目标业务应该如何运行、哪些问题必须解决   | 现网已经如此、候选设计已经可实现 |
| `S`        | 行业规范、法规、标准或权威外部说明                     | 在明确版本、地区和适用范围内的外部约束   | 本项目已采用或现网已满足         |
| `R`        | 现网代码、数据、样本、表单或运行记录                   | 当前或历史上实际出现过什么               | 该做法就是目标方案或普遍规律     |
| `C`        | 尚待验证的设计候选                                     | 可供冲突分析、原型、契约或样本验证的方案 | 已获批准的正式规则               |
| `Decision` | 经过冲突分析、边界验证和代表性样本回验后批准的正式决策 | 在记录的范围、版本和生效时间内约束实现   | 超出适用范围的永久真理           |

`D1...Dn` 只是评审清单里的追踪编号，不自动等于 `Decision`。正式决策至少记录负责人、证据、冲突与替代方案、适用边界、样本结果、影响和生效版本；`O/S/R` 之间没有可脱离语境的固定优先级，发生冲突时必须说明责任主体和适用范围。

原有 Excel/表单属于 `R` 级业务证据与需求素材：用于提取字段、动作、责任、时序和统计口径，不作为新系统的表结构或菜单模板。目标模型以货柜、节点、任务、事件、费用、异常和决议等统一对象为底，再按角色生成任务视图、计划视图和管理投影，禁止把重复填报原样电子化。

## 2. 超期费用管理的纳入口径（负责人确认类型边界）

- **纳入核心范围**：围绕业务俗称“滞港费”的超期费用，分别建立 Demurrage、Detention、Storage 的免费期(Last Free Day)、费率标准、费用预估/预警、对账与一键处置闭环；对接到港/卸船/可提/提柜/还箱等适用事件与异常预警。
- 依据/资产：现网 `ext_demurrage_standards`（目的港+船司+免费期+费率+币种，LAX 免5天 Demurrage 80 USD/天）与 `ext_container_charges`（费用记录）→ 迁移与复用输入；外部规范 `SRSD/SRSE`(运费滞留/结清)、码头超期滞留预警 作对标。
- **计费框架（负责人确认边界，具体规则待样本定稿）**：Demurrage、Detention、Storage 分别选择费用责任段、起算/截止事件、免费期、自然日/工作日、是否含首尾日、阶梯费率、币种、生效区间和账单权威；不能固化为“到港至提柜天数 × 单一日费率”。**预计**由事件/计划与合同标准计算，**账单金额**来自服务商单据，**审核确认金额**由对账结果产生，三者不得互相冒充；清单见 [FEE_DEMURRAGE](./domain/FEE_DEMURRAGE.md)。
- 尚未纳入（保持候选，另审）：其他费用种类（海运费核对除外）、扣货/罚款等——如需纳入另走评审。
- 关联动作：`notify_delay`/`dispatch_exception`/`schedule_empty_return`（ACTION_CATALOG）；K7 免费期剩余预警（NODE_PDCA）。

## 3. 应用规则

- 方案评审以此清单为验收锚（M0 + P1–P10，见 [PROPOSAL_SELF_AUDIT](./domain/PROPOSAL_SELF_AUDIT.md) A 类矛盾回查）。
- 涉及外部事实/来源权威、事件乱序/补录/分支、服务器确认三档、费用类型化与复核触发、表单数字化为投影时，逐条对照 P5–P10 应用约束。
- 涉及货柜、任务、扫描提交或管理指标的设计，必须逐项核对 §1 的三种状态与“管理维度 × 主流程节点 × 作业动作”映射。
- 每个主流程节点必须按 §1.6 七组 SOP 核对其计划、执行、状态、周期、费用、异常改善和会议消费关系；不适用项也须明确说明。
- 七组不得机械生成七张表或七个页签；现场员工默认只见任务上下文、当前可执行动作、必要证据、结果/异常入口和同步反馈。领取、物料、扫描、报工不是所有节点的固定步骤。
- 所有写操作按 §1.1 的最小三段确认检查；是否增加更细状态必须用责任、事务、恢复或用户动作差异证明必要性。
- 外部事实按 §1.7 与 INTEGRATION_BOUNDARIES 的字段级权威规则接入；乱序、异常分支、更正、重放、对账与人工纠偏必须可追踪。
- 所有方案按 §1.8 标记 `O/S/R/C/Decision`；没有通过决策门禁的候选不得进入正式契约或实现。
- 术语/口径漂移即时回正；不因单点便利牺牲原则清单之任一（M0 + P1–P10）。
