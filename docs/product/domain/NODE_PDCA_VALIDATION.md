# 历史 8 关键节点 · 外部样本回验底稿（NODE_PDCA_VALIDATION）

> 状态：**历史评审输入（当前不可复现，不得作为验收证据）** · 2026-09-04 · 2026-09-10 复核 · 负责人：刘志高。
> 方法：用现网 `D:/Github/logix`（锚点 `main acfb50a8`）的**种子/样例**回验 [NODE_PDCA](./NODE_PDCA.md) 的 8 关键节点
> （要素可用性 → 阈值候选 → 缺口清单）。**样例为示例数据，仅供结构对照，非真实业务分布**；阈值最终须以 P2-12 真实脱敏样本 + P8 实测校准。
>
> 可复现性说明：当前仓库及本机 `D:/Github` 均不存在上述 `logix` 仓库和锚点文件；现行 NODE_PDCA 已采用“七组 SOP + 14 节点”，不再使用 K1–K8 作为结构。以下内容仅保留历史调查线索，必须重新引入经授权、脱敏、可追踪的样本和验证脚本后才能引用为证据。

## 1. 历史底稿列举的样本资产（当前不可用）

| 源                                           | 内容                                                                                         | 用于                        |
| -------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------- |
| `backend/scripts/init-database.sql`          | 历史底稿声称包含备货单、货柜、海运、港口作业、字典、身份种子和滞港费标准；原文件当前不可访问 | K1/K2/K3/K4/K5/K6/K7、F1–F6 |
| `backend/scripts/init-database-complete.sql` | dict_ports/shipping_companies/container_types 扩展字典（含 scac、max_weight_kg 等）          | 字典初值                    |
| DDL/实体（03_create_tables、entities）       | 各表权威列（含 ext_demurrage_standards、trucking/warehouse/empty 列集）                      | 要素→字段映射               |

**明确缺口（真实业务样本）**：无真实脱敏 装箱/拖卡/仓库/还箱/异常/中转/标记(SKU) 样本；P2-12 尚未建（RAID R-05 占位）。

### 1.1 历史底稿声称的现网样本（当前未复核）

- **历史底稿声称的全链样本（业务标识和数值已省略）**：原引用位于当前不可访问的 `public/docs-temp/`，仅记录到订单、海运、清关、到港、仓库和还箱字段存在缺失及部分时序倒挂。
- **历史底稿声称的到港分布快照**：原 `debug-arrival-distribution.sql` 当前不可访问，只能作为待重新验证的调查线索。
- **历史底稿声称的合柜证据**（见 §2 F7）：原引用与样本当前均不可访问，不能据此确认旧库基数。

**历史观察（未复核）**：底稿称柜级重量、品名、封条、清关实际日期和卸柜日期等字段存在缺失，日期精度与时序也有异常。“要素是否齐全”仍应作为新样本预检项，但不得引用本底稿计算缺失率。

## 2. 回验发现（需负责人裁决/关注）

| #   | 发现                                                                                    | 影响                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | 历史种子曾显示同一代表备货单号关联两个货柜                                              | 历史底稿曾解释为票级代表字段误读，但源 DDL、行级数据与解释依据当前均不可访问；不得据此确认旧库基数。TO-BE 1:1 仍须经迁移探查验证              |
| F2  | 种子含 `sys_users/roles/user_roles`，但运行时代码（entities/middleware）无身份/鉴权使用 | 身份是**半成品**（schema 有、代码无）→ P5 迁移输入（补充 ASIS 判断：非"完全没有"，而是"未接线"）                                              |
| F3  | 历史底稿称存在按目的港、船司、免费期、费率和币种维护的滞港费/堆存费标准                 | 可作为 G4 费用对象的待验证输入；在源表复核前不得称为现网资产                                                                                  |
| F4  | 历史底稿称柜型、港口和船司字典包含业务代码                                              | 重新取得经授权数据后可评估为字典别名初值（G3/P2-04）                                                                                          |
| F5  | 历史底稿称 `sea_freight.ata` 与其他到港时间字段存在语义冲突                             | 作为 A8 时间口径的待验证问题；planned/actual 仍须逐节点厘清                                                                                   |
| F6  | 样例缺 中转/装箱时间/拖卡/仓库/还箱/标记(SKU)数据；无危险品·植检·温控样例               | 构成 P2-12 采集清单                                                                                                                           |
| F7  | 历史底稿引用过“一柜多单/合柜”文档与行级样本                                             | 原文和样本当前不可访问，既不能确认合柜，也不能确认其只是代表字段误解。TO-BE 不建模一柜多单；施加唯一约束前必须按迁移阻断项核验旧库 DDL 与数据 |
| F8  | 历史底稿称单柜字段缺失、日期精度不足且部分时序倒挂                                      | 仅作为待验证风险；阈值定稿须使用新的授权脱敏样本并包含完整率口径                                                                              |

## 3. K1–K8 逐节点回验

| 节点         | 历史底稿声称的可用要素                                          | 缺口（样例未覆盖）                                                       | 阈值候选（临时，待样本定稿）       | 定稿状态 |
| ------------ | --------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------- | -------- |
| K1 备货就绪  | 备货单号、客户、国别、金额、预计/实际出运日期、件数、CBM 和毛重 | 供应商备货完成时间/就绪标志；货量与后续装柜数回对                        | 延期阈值须用新样本重算             | 待样本   |
| K2 装箱定稿  | 历史底稿列举柜型、箱号、封号、毛/净重、CBM、件数和品名          | 装箱时间/提空箱时间；真实 vs 计划；危险品/植检/致冷剂/超限样例；打托装配 | 体积和毛重利用率阈值须用新样本重算 | 待样本   |
| K3 出运确认  | 订舱号、B/L、船名航次、船司、POL/POD 和 ETD                     | 出运确认单证/船期变更/甩柜样例；`shipment_date` 未在此种子子集           | 无（需船期变更分布）               | 待样本   |
| K4 离港      | ATD 与 ETD 对照                                                 | ATD 晚于 ETD 的分布样本；截关证据                                        | 偏差阈值须用新样本重算             | 待样本   |
| K5 在途/中转 | origin/destination 港口作业、eta/ata                            | **中转（transit）样例缺失**；ETA 漂移/滞留样例                           | 无（需中转/漂移分布）              | 待样本   |
| K6 清关      | 清关状态、ISF 状态和最后免费日                                  | 计划/实际清关日期字段值；查验/扣货样例                                   | 放行→拖卡前置；免堆剩余预警见 K7   | 待样本   |
| K7 到港→提柜 | 预计/实际到港、最后免费日和滞港费标准字段                       | ATA/可提时间、派拖记录、滞留样例                                         | 免费期与派拖超时阈值须用新样本重算 | 待样本   |
| K8 卸空→还箱 | （无）                                                          | 拖卡送达/卸柜/卸空/还箱全部缺失；WMS 收货回传样例                        | 无                                 | 待样本   |

## 4. 汇总与行动

- **样例可用度**：K1–K4/K6/K7 有结构样例；K5 中转、K8 后段、标记(SKU) 完全缺样例 → 需 P2-12 采集。
- **F1/F7 当前未复核**：历史底稿给出的 1:1 解释缺少可访问的源 DDL、行级数据和原始调查材料。TO-BE 仍选择一备货单→一柜、一提单→多柜，但数据库约束与数据修复必须等待迁移探查结论。
- **阈值统一注记（评审 A4）**：本文 95% / 免费期≤2d / 48h 等阈值均为**候选**，未样本/评测证实，不得作验收承诺；NFR 正式值待 P7/P8。
- 采集清单（喂 P2-12）：真实脱敏 装箱（含危险品/植检/致冷剂/超限 SKU）、拖卡派单、仓库卸柜/卸空/收货、还箱、中转、甩柜/扣货异常、时间偏差（atd−etd、eta−ata、实际 vs last_free）。
- 阈值候选均为临时值；定稿口径 = P2-12 样本分布 + P8 实测（RAID R-05、NFR §4/§7）。

## 5. 关联与维护

- 关联 [NODE_PDCA](./NODE_PDCA.md)、[TARGET_FIELD_CATALOG](./TARGET_FIELD_CATALOG.md)（要素→字段）、[P2_REVIEW_CHECKLIST](./P2_REVIEW_CHECKLIST.md) G5、AS-IS [快照](./AS_IS_LEGACY_BASELINE.md)。
- 建议补记 AS-IS 快照：`ext_demurrage_standards`、身份种子(半成品)、`sea_freight.ata` 语义待厘。
- P2-12 真实样本到位后：替换 §3 缺口、把 §4 阈值转正式并留痕（NFR §7）。
