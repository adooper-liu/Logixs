# 全局公共契约登记表 V1

> 状态：治理基线  
> 版本：1.0  
> 最后更新：2026-09-10
> 适用范围：货柜全生命周期、14 节点主链及各限界上下文

## 1. 目的与边界

本登记表是全局公共契约的唯一治理入口，用于跟踪契约所有权、成熟度、权威文档、消费者、阻断项和下一动作。它只记录治理元数据和已批准决策的索引，不复制状态枚举、事件映射、字段定义或业务规则；具体定义始终以“权威文档”列指向的契约为准。

登记为“已确认决策”只表示该项规则已获得业务选择，不代表所属契约整体已经批准、实例化或发布。

## 2. 阶段门禁

| 门禁 | 名称            | 进入条件                                                                                                        |
| ---- | --------------- | --------------------------------------------------------------------------------------------------------------- |
| `D0` | 未开始          | 尚无明确范围、所有者或权威草案。                                                                                |
| `D1` | 设计中          | 已明确范围和候选规则，正在形成独立权威契约。                                                                    |
| `D2` | 待负责人评审    | 契约文档已具备完整评审内容，尚未取得全部必要批准。                                                              |
| `D3` | 业务批准        | 业务语义、所有者和跨模块边界已正式批准，遗留项不影响实例化。                                                    |
| `D4` | Schema 已实例化 | 已形成唯一公共 JSON Schema 源及目录，并完成 Schema、引用、目录属性和 fixture 自校验；不代表其他技术载体已实现。 |
| `D5` | 技术载体已实现  | DTO、OpenAPI、生成类型、运行时边界校验、数据库显式映射、迁移、Adapter 和消费者已按契约实现。                    |
| `D6` | 验证通过        | 契约、数据库、API、集成和关键流程验证均通过且证据可追溯。                                                       |
| `D7` | 已发布          | 已进入目标环境，具备版本、监控、兼容与回退记录。                                                                |

门禁原则上单调推进。任何回退都必须记录原因、影响和重新准入条件。`D4` 至 `D7` 必须附可验证的产物或检查链接；没有证据不得自行升格。

## 3. 契约总表

| ID       | 全局契约 / 权威入口                                                             | 所有者           | 优先级 | 当前门禁 | 业务批准 | 实例化 / 持久化 / 验证                       | 主要消费者                                  | 当前阻断                            | 下一动作                                      |
| -------- | ------------------------------------------------------------------------------- | ---------------- | ------ | -------- | -------- | -------------------------------------------- | ------------------------------------------- | ----------------------------------- | --------------------------------------------- |
| `GC-001` | [生命周期节点目录 V1](./LIFECYCLE_NODE_CATALOG_V1.md)                           | 生命周期域负责人 | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 状态机、任务工单、查询投影、全部业务模块    | G7 派生与运行时实现尚未完成         | G7 生成类型、Seed 与消费者对拍                |
| `GC-002` | [货柜生命周期状态机契约 V1](./CONTAINER_LIFECYCLE_STATE_MACHINE_CONTRACT_V1.md) | 生命周期域负责人 | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 所有节点模块、Application、API、前端操作台  | G7 派生与运行时实现尚未完成         | G7 生成状态机类型并完成消费者对拍             |
| `GC-003` | [规范事件目录 V1](./EVENT_CODES.md)                                             | 事件治理负责人   | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 状态机、时间线、Adapter、任务工单、投影     | G7 派生与运行时实现尚未完成         | G7 生成事件类型、Adapter fixture 与消费者测试 |
| `GC-004` | [货柜生命周期时间线契约 V1](./CONTAINER_LIFECYCLE_TIMELINE_CONTRACT_V1.md)      | 生命周期域负责人 | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 海关、船期、港口、拖卡、仓库、查询投影      | G7 派生与运行时实现尚未完成         | G7 生成时间线类型并完成消费者对拍             |
| `GC-005` | [任务与工单契约 V1](./TASK_WORK_ORDER_CONTRACT_V1.md)                           | 任务工单域负责人 | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 各节点子任务、工单、状态机、操作台          | G7 派生与运行时实现尚未完成         | G7 生成任务工单类型并完成消费者对拍           |
| `GC-006` | [证据与来源权威契约 V1](./EVIDENCE_SOURCE_AUTHORITY_CONTRACT_V1.md)             | 证据治理负责人   | P0     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 外部 Adapter、人工补录、状态机、审计        | G7 派生与运行时裁决器尚未完成       | G7 生成证据类型并完成消费者对拍               |
| `GC-007` | [跨模块引用契约 V1](./CROSS_MODULE_REFERENCE_CONTRACT_V1.md)                    | 架构负责人       | P1     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 全部限界上下文、事件、审计和投影            | G7 数据库映射与运行时对账尚未完成   | G7 生成引用类型并完成映射对拍                 |
| `GC-008` | [动作与权限契约 V1](./ACTION_PERMISSION_CONTRACT_V1.md)                         | 权限治理负责人   | P1     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | Application、API、授权、前端操作台、审计    | G7 服务端授权与动作目录尚未完成     | G7 生成动作类型并完成授权实现对拍             |
| `GC-009` | [同步可靠性契约 V1](./SYNC_RELIABILITY_CONTRACT_V1.md)                          | 集成平台负责人   | P1     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | 外部 Adapter、Inbox、Outbox、任务工单、运维 | G7 数据库表与运行时发布器尚未完成   | G7 生成可靠性类型并完成运行时对拍             |
| `GC-010` | [查询投影契约 V1](./QUERY_PROJECTION_CONTRACT_V1.md)                            | 查询体验负责人   | P1     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | API、Web 操作台、报表、移动端               | G7 投影器、API 与前端消费者尚未完成 | G7 生成查询类型并完成消费者对拍               |
| `GC-011` | [公共错误契约 V1](./PUBLIC_ERROR_CONTRACT_V1.md)                                | 平台契约负责人   | P1     | `D4`     | 已批准   | Schema 已实例化 / 未实现 / Schema 门禁已验证 | API、前端、Adapter、审计和运维              | OpenAPI 与运行时异常映射未实现      | G7 派生错误类型并实现 parity                  |

当前汇总：`D4` 11 项，`D1` 至 `D3`、`D5` 至 `D7` 0 项。

## 4. 已确认决策索引

以下决策进入相关契约评审基线，详细语义仍以对应权威契约为准：

- `cargo_ready` 按“一备货单一次有效确认”；可由人工、ERP、供应链系统或导入确认。
- 箱号尚未产生时不进入货柜生命周期主管道，只处于备货管理状态。
- `rail_handover` 表示铁路主体或场站实际接收货柜，不表示仅接单或预约。
- 规范事件保留 `completionEligibleNodeCodes`；事件接收按 `eventId` 幂等，节点事实应用按 `(eventId, targetNodeInstanceId)` 幂等。
- `sailing` 不完成节点；`transit_arrived` 可申请完成 `ocean_transit`；`arrived` 可申请完成 `ocean_transit` 与 `destination_arrival`。
- `delivered` 和 `warehouse_arrival` 均可申请完成 `warehouse_delivery`；前者要求 POD 或签收证据，后者要求仓库、WMS 或门岗权威来源。
- `container_customs_completed` 可申请完成 `customs_clearance`；单案卷 `release` 本身不直接完成货柜节点。

## 5. 推荐推进顺序

```text
GC-001 / GC-003 已批准
  -> GC-002 / GC-004 已整体批准
  -> GC-005 任务与工单契约（已批准）
  -> GC-006 证据与来源权威契约（已批准）
  -> GC-007 跨模块引用契约（已批准）
  -> GC-008 动作与权限契约（已批准）
  -> GC-009 同步可靠性契约（已批准）
  -> GC-010 查询投影契约（已批准）
  -> GC-011 公共错误契约（已批准）
  -> 公共 JSON Schema 单一源及派生实现
```

依赖契约可以并行起草，但不得绕过其上游语义批准进入 `D4`。

### 5.1 P6.1 签署记录

| 评审项 | 契约               | 业务签署                   | 技术边界复核 | 日期       | 结论                                                                                                             |
| ------ | ------------------ | -------------------------- | ------------ | ---------- | ---------------------------------------------------------------------------------------------------------------- |
| P6.1   | `GC-002`、`GC-004` | 刘志高（生命周期域负责人） | Codex        | 2026-09-10 | 整体批准，`D2 -> D3`；行为变更仅适用于新建 V1 流程或经审计迁移后的流程；Schema、实现、迁移和运行验证不随签署升格 |

### 5.2 G6 Schema 实例化记录

| 日期       | 范围            | 权威入口                                   | 验证命令              | 证据                                                                                   |
| ---------- | --------------- | ------------------------------------------ | --------------------- | -------------------------------------------------------------------------------------- |
| 2026-09-10 | `GC-001`～`011` | `packages/contracts/schemas/v1/index.json` | `pnpm contract:check` | 14 个 Draft 2020-12 Schema、14 节点、32 事件、8 场景及 21 个正负向 Schema fixture 通过 |

## 6. 更新规则

1. 每项契约只有一个 `contractId`、一个业务所有者和一个权威入口；没有明确负责人时不得进入 `D3`。
2. 负责人字段登记角色，评审记录另行登记具体签署人、日期和结论。
3. 契约负责人在状态、阻断、下一动作或权威入口变化时更新本表，并同步最后更新日期。
4. 枚举、事件载荷、业务守卫、错误码和字段定义不得复制到本表；发现不一致时以已批准的权威契约为准并发起纠偏。
5. `D3` 必须有业务与技术边界共同批准记录；`D4` 必须通过 JSON Schema、目录属性、引用与 fixture 自校验；`D5` 必须通过跨语言、OpenAPI、运行时边界和数据库映射一致性检查并关联迁移/实现；`D6` 必须关联可复现验证结果。
6. 新增全局契约须先证明其跨越三个或以上模块，或属于稳定的安全、数据、集成边界；模块私有规则不得进入本表。
7. 版本升级必须说明兼容性、消费者影响和迁移策略，不得原地改写已发布语义。

## 7. 实施准入检查

任何依赖全局契约的开发任务进入实现前，至少确认：

- 所依赖契约已达到任务要求的门禁，且权威入口唯一可访问。
- 任务 brief 明确引用 `contractId` 和版本，不在模块内重新定义公共枚举或规则。
- 若契约尚低于 `D3`，任务只能制作原型、验证假设或完善契约，不得将候选语义作为生产契约实现。
- 涉及公共边界时，已规划 Schema、DTO、OpenAPI、数据库映射和消费者的一致性验证。
