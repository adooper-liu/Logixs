# 错误码目录（兼容导航）

> 状态：**已由 GC-011 取代** · 2026-09-10

公共错误信封、稳定错误码、HTTP 映射、冲突类型、可重试性和兼容策略的唯一权威是[公共错误契约 V1](./PUBLIC_ERROR_CONTRACT_V1.md)。

本文件原有 v0.1 候选码未发布为 V1，只作为迁移输入保留在版本历史中。新 API、Schema、DTO、前端分支和 Adapter 不得继续使用或复制旧 `VAL_*`、`AUTH_*`、`BIZ_*`、`IDEM_*`、`NF_*`、`EXT_*` 线值；P6 必须用迁移 fixture 明确旧候选到 GC-011 的映射。
