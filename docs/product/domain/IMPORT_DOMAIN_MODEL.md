# 导入领域模型（IMPORT_DOMAIN_MODEL · v1.1 过渡说明）

> 状态：**当前实现说明 v1.1；目标业务入口已变更，禁止按旧模型扩展** · 2026-09-23 · 负责人：刘志高。
> 一句话：现有批次、原文件、审核、预检和对账基础保留，但 Adapter 只生成统一 `ShipmentHandoffCommandV1`，由 Shipment 接收边界提交可证明的已出运事实。
> 当前旧格式仍按备货单号聚合并创建 `not_shipped` 货柜；这只是实现事实，不是目标业务规则。目标差异和迁移门禁统一见 [`POST_DEPARTURE_CORE_MODEL_GAP_V1`](./POST_DEPARTURE_CORE_MODEL_GAP_V1.md)。
> 证实度 S·R·O·C。流程状态(批次)正文在 LIFECYCLE/契约，不复制。

## ⓪ 边界修正

| 项             | 当前实现                               | 目标                                                                    |
| -------------- | -------------------------------------- | ----------------------------------------------------------------------- |
| 正式分组锚     | `tenantId + orderNumber`               | 稳定外部交接/Shipment 身份；无 Shipment ID 时进入人工匹配，不自动拼键   |
| 正式写入对象   | 备货单、产品行、兼容货柜、柜级时间事实 | Shipment、出运货物、货柜关系、运输单证、上游引用和事件事实              |
| 准入           | 可创建 `not_shipped` 柜记录            | 必须有 ATD、权威 `DEPARTED` 或带依据的授权人工确认                      |
| 生命周期初始化 | 每个导入柜立即初始化 Flow              | Shipment 正式落账后，按关联柜初始化/复用柜级 Flow，并建立 Shipment 投影 |
| 上游对象       | 备货单是导入主对象                     | 备货单仅是可选上游引用/血缘                                             |

迁移完成前，以下正文的“旧格式”规则用于解释现有代码和兼容数据，不能作为新导入契约继续扩展。

## ① 可落库清单

### A. 对象与值表（枚举/状态）

| 对象/枚举      | 值                                                              | 含义                                   | 证实 |
| -------------- | --------------------------------------------------------------- | -------------------------------------- | ---- |
| 批次状态(流程) | pending…executing…completed / failed·rejected·cancelled·expired | 引用 LIFECYCLE/契约                    | O    |
| 审核决策       | approved / modified / rejected                                  | 采纳原样 / 修正后执行 / 拒绝(可重映射) | O    |
| 行处置         | skipped                                                         | 整行跳过                               | O    |
| 行执行结果     | success / skipped / failed / duplicate                          | 成功带 orderNumber + line 引用回查     | O    |
| 匹配判定       | hit=更新 / miss=新建 / dup=同一来源行重放                       | 主锚 orderNumber；重复在明细粒度判断   | O    |
| 原文件留存状态 | not_retained / retained                                         | 历史未留存 / 对象存储已留存            | O    |

### B. ImportBatch 结构（聚合）

| 部分              | 内容                                               | 说明                                           |
| ----------------- | -------------------------------------------------- | ---------------------------------------------- |
| ImportBatch       | 幂等键 · 解析版本 · 替代血缘 · 原文件引用 · 统计   | 一次上传全生命周期；替代批次只追加、不覆盖旧批 |
| ImportRow         | rowNo · 原始快照 · 分节内容 · 审核 · 执行结果      | 一行=一个来源记录；产品表中通常是一条产品明细  |
| 映射建议(AI 产物) | 源列→标准字段 · evidence · confidence · provenance | 不可变留痕                                     |
| 预检              | 违规项 · blocker 禁写                              | PRECHECK                                       |
| 对账              | 幂等/重复/差异报告                                 | 回查批次                                       |

### C. 旧格式导入匹配/去重判定表（分组锚=备货单号）

| 情形                                   | 判定      | 行为                                          |
| -------------------------------------- | --------- | --------------------------------------------- |
| 同 orderNumber 命中库内记录            | hit       | 更新该 ContainerRecord（含状态列/迟绑定回填） |
| orderNumber 未命中                     | miss      | 新建一条                                      |
| 同文件内多行同 orderNumber、产品行不同 | line      | 聚合一个表头，逐行写入产品明细                |
| 同 orderNumber 的表头字段不一致        | conflict  | `HEADER_CONFLICT`，不猜值、不取第一行         |
| 同一来源行重放或业务明细完全重复       | duplicate | 按来源身份/已确认明细键去重，不只看产品货号   |
| 双方已带箱号且不一致 / 旧误解数据      | 异常      | 进对账/人工，不静默（DATA_CLEANUP）           |

### D. 四类对象追踪（P2 门禁）

| 对象              | 本质            | 存放/键                 | 单向链                 |
| ----------------- | --------------- | ----------------------- | ---------------------- |
| AI 产物(映射建议) | 非事实          | Row 建议 + provenance   | 产物→…                 |
| 审核结果          | 人工处置        | 审核项(operator/reason) | 指向产物               |
| 执行结果          | 写入结局        | 行结果 + orderNumber    | 指向审核               |
| 业务事实          | ContainerRecord | Shipment 域             | 记录来源批次/行/操作者 |

## ② 定义与澄清

- 导入只“把数据可靠放进来”，**不拥有业务事实**。当前兼容端口是 `applyReplenishmentOrderImport`；目标只允许调用 `PreflightShipmentHandoffPort` / `AcceptShipmentHandoffPort`，契约及原子边界见 [`POST_DEPARTURE_CORE_MODEL_GAP_V1` §5.10](./POST_DEPARTURE_CORE_MODEL_GAP_V1.md)。
- 批次=一批文件；来源行不等于货柜。产品明细文件按 `tenantId + orderNumber` 聚合成一个备货单和兼容货柜表头，行级字段逐条落产品明细；后续 SKU 绑定和装载分配走独立公开 Port。
- AI 只产"建议"，全部人工确认后才预检/写入（首批策略）。
- 文件「物流状态」列：表达来源方声明的实际状态而非计划/预计；归一后作为状态证据，必须经 D7 字段/事件权威、证据一致性和状态机合法转换校验。通过后由业务事件推进 `currentStatus`，冲突进入对账/复核。
- 当前用例会建立 `not_shipped` 柜记录，这是已登记的目标差异。新核心入口不得先建出运前记录再等待推进；只有合格的实际出运证据通过后才能创建正式 Shipment。
- 目标入口允许四类来源任意子集到达。文件上传/解析按单文件原子，多文件结果互不回滚；联合归组后的业务冲突按柜隔离，不能用“来源包不完整”阻断所有已到数据。
- 除内部主键、必需外键/租户引用和对象身份冲突外，缺失字段进入待补事项并允许 Shipment 从 `departed` 开始。无 SKU 明细是货物完整度缺口，不是导入失败或全流程阻断。

## ③ 规则与约束/边界

- 批次幂等：同 idempotencyKey 再次上传 → 返回原批次不重写。
- 新批次原文件先进入 MinIO/S3，再创建批次与解析行；PostgreSQL 只保存对象键、SHA-256、MIME、字节数和留存时间，不保存文件体。
- 对象键由服务端批次 UUID 生成，不包含原文件名或租户名称；幂等命中不重复上传，建批失败只删除本批独占对象。
- 历史批次没有原文件证据时必须标记 `not_retained`，不得根据解析行反向拼装或伪造原文件。
- 重新解析/修正采用替代批次：操作人重新提供文件，新批次记录 `parserVersion + replacesBatchId`，重新走映射、审核、预检和执行；旧批次及其行、建议、审核、结果保持不可变。
- 有效幂等范围包含解析器版本和被替代批次；同版本同文件同替代目标的重试返回同一新批次，解析器升级后允许同一文件形成新快照。
- 任何改变表头识别、来源行快照或版式判定的发布必须提升 `parserVersion`；仅重构且输出语义不变时保持版本。
- 单个表格区域只承载一种行粒度；横向产品明细与纵向货柜字段值块混排时，解析阶段以 `LAYOUT_MIXED` 阻断，不把详情字段伪装成产品行。不同实体/粒度应拆分 Sheet 或使用显式命名区域。
- 预检 blocker → 禁止写业务表（硬约束）。
- `ContainerRecord.orderNumber/replenishmentOrderId` 仅保留当前导入兼容语义；不得据此拒绝一柜多单或一条明细拆入多柜。
- 同一备货单可有 N 条产品明细；`productNumber` 不能单独作为明细唯一键。
- 已提供装载分配时必须先核验并绑定 Product/SKU；未提供时建立 `cargo_detail_incomplete` 待补事项。导入不得在事务外静默创建 SKU、从整柜汇总反推 SKU 或猜测历史行绑定。
- `shippedQuantity + quantityUnit` 与整单 `packageCount` 独立保存；缺单位不得因当前样本与箱数相等而默认成“箱”。
- 同一备货单聚合必须事务落账：表头与全部有效产品明细同成同败；不同备货单可分别记录结果并继续执行。
- 实际时间遵守 LIFECYCLE_CONSISTENCY R10–R12；来源状态缺实际时间、来源 UTC 偏移不明、来源系统/证据缺失或推导值冒充 actual 时预检阻断；推导值使用 `estimated + system_derived` 并带规则版本。
- V1.2 时间事实保留原值、来源 UTC 偏移、UTC 标准值、来源系统、来源状态、证据/推导规则和来源批次/行；只落领域事实，不自动生成规范事件或推进状态。
- 状态列归一须过字典；未知字典值进待处理（不静默）。
- **目标规则（当前代码尚未实现）**：整批数据必须能证明 Shipment 已到实际出运节点或合法后继节点；状态未知、仍在出运前或与证据冲突时，必须阻断或转人工。
- 写入一次、逐行记结果；行失败不影响其它行。
- 写前过 来源权威(D7)/可写窗口/密封(LIFECYCLE)。

## ④ 流程（怎么走）

当前兼容链路：上传 → 解析 → 映射/审核 → 按备货单预检 → 逐备货单事务写入兼容记录 → 柜级 Flow 初始化 → 行结果。

目标链路：任一来源上传/接入 → 单文件原子保存原始证据 → 跨已到来源归组货物、柜和单证 → 生成 `ShipmentHandoffCommandV1` → 统一 preflight → 逐柜处理冲突/生成待补项 → accept 可识别 Shipment → 逐对象结果 → Outbox/Inbox 初始化 `departed` 生命周期并由专业域吸收来源切片。

## ⑤ 注意事项（坑）

- 别把 AI 建议当事实直接落业务表。
- 别跳过人工审核/预检硬闸当快捷方式。
- 别用主备货单号或箱号做唯一键（迟绑定/票级代表）。
- 别让 Excel Adapter、未来 packing Adapter 或人工入口各写一套 Shipment 落库逻辑。
- 状态文本列别静默吞成 not_shipped（老反例 A1）。
- 别把出运前或无法确认是否已出运的行混入当前列表。
- 同一记录多次导入=更新，别误建多行。
- 别在旧批次上原地重跑新解析器或覆盖旧建议；重新提供文件并创建带血缘的新批次。
- 别把对象键、bucket、endpoint 或存储凭据暴露给浏览器；下载/重解析必须由服务端鉴权后读取。
- 别把同备货单多产品行当重复，也别在尚未支持产品明细时放行并静默丢弃第二行以后数据。
- 别把同一 Sheet 里的纵向货柜详情当成横向产品明细；先拆分数据区域，再分别按明确粒度导入。
- 别用状态、导入时刻或规则计算结果补造实际清关/卸柜/卸空时间。

## ⑥ 白话注解（🗣️）

🗣️ 导入人员交进来的不是“几张备货单”，而是一批已经出发的货。系统要先确认这是哪一票、有哪些柜、每个柜装了什么以及凭什么认定已出发；确认通过后才正式入库。

## ⑦ 落库映射

| 清单              | 落库                                                       | 示例                             |
| ----------------- | ---------------------------------------------------------- | -------------------------------- |
| 批次/行/审核/结果 | import_batch · import_row · review · row_result            | orderNumber                      |
| 决策/结果枚举     | enum/字典                                                  | approved                         |
| 匹配判定          | 应用逻辑(orderNumber 聚合 + 来源行/明细去重)               | hit/miss/line/dup                |
| AI 产物           | ai_artifact 记录                                           | mapping suggestion               |
| 幂等              | idempotency_key 唯一                                       | source+hash                      |
| 原文件证据        | MinIO/S3 文件体 + import_batch 元数据                      | retained+sha256                  |
| 时间事实          | shipment_time_fact（版本化 actual/estimated）              | 原值+偏移+UTC+来源               |
| SKU/装载后续      | product_sku_id + container_cargo_allocation_set/allocation | 先绑定 SKU，再版本化确认箱货关系 |

## ⑧ 待评审/关联

- 2026-09-23：批次基础设施保留；备货单字段目录、分组逻辑、正式写端口和柜流程初始化均需按 Shipment 目标重构。迁移评审通过前不继续扩大旧端口。
- 关联：CONTAINER_STATUS_MODEL(状态文本)、PRECHECK_RULES、DATA_CLEANUP_ORDER_CONTAINER、FIELD_MIGRATION_MAP、TARGET_FIELD_CATALOG、EXTERNAL_EVENT_MAPPING。
